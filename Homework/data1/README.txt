This is a module used to verify the difference between bug changes in the Linux kernel and fix changes.
Be sure that you have aquired record.txt before calling hypothesis2.py.
To get record.txt, you can use "git --no-pager log -100 --grep 'Fixes:' > record.txt"
